package com.yashitech.html_to_figma_api;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * HtmlToFigmaApiApplication.java — Spring Boot application entry point.
 * @SpringBootApplication enables auto-configuration, component scanning, and configuration.
 */
@SpringBootApplication
public class HtmlToFigmaApiApplication {
    public static void main(String[] args) {
        SpringApplication.run(HtmlToFigmaApiApplication.class, args);
    }
}
